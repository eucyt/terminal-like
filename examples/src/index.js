import React from 'react'
import { render } from 'react-dom'
import TerminalContent from '../../dist'

const App = () => <TerminalContent />
// eslint-disable-next-line no-undef
render(<App />, document.getElementById('root'))
